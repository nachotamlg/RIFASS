{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "node scripts/init-db.js && npx prisma generate && next start",
    "restartPolicyMaxRetries": 5,
    "restartPolicyType": "on_failure"
  }
}

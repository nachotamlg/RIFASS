{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "node scripts/migrate.js && next start",
    "restartPolicyMaxRetries": 5,
    "restartPolicyType": "on_failure"
  }
}

{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "node scripts/bootstrap.js && next start",
    "restartPolicyMaxRetries": 5,
    "restartPolicyType": "on_failure"
  }
}
